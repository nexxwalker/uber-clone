{
  "name": "uber-clone",
  "private": true,
  "scripts": {
    "build": "if [ ! -d \"flutter\" ]; then git clone --depth 1 --branch stable https://github.com/flutter/flutter.git flutter; fi && cd uber_users_app && ../flutter/bin/flutter build web --release --base-href /"
  }
}
